#ifndef IMPRIMIR_RECURSIVA_LISTA_H
#define IMPRIMIR_RECURSIVA_LISTA_H

/*
void lerListaRecursiva(pNoh atual, int pos, FuncaoImpressao pfi){
    if(atual != NULL){
        pfi(atual->info, pos+1);
        lerListaRecursiva(atual->prox, pos+1, pfi);
    }
}

void imprimirListaRecursiva(pDLista pd, FuncaoImpressao pfi){
    pNoh atual = pd->inicio;
    lerListaRecursiva(atual, 0, pfi);
}
*/
#endif